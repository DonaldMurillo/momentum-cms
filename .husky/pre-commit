#!/bin/sh

# Run lint-staged (eslint + prettier on staged files)
npx lint-staged

# Type-check affected projects (strict, single source of truth)
npx nx affected -t typecheck --parallel=3

# Build affected projects
npx nx affected -t build --parallel=3
