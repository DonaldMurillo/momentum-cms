# server-analog

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build server-analog` to build the library.

## Running unit tests

Run `nx test server-analog` to execute the unit tests via [Jest](https://jestjs.io).
