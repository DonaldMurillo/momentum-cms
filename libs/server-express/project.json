{
	"name": "server-express",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "libs/server-express/src",
	"projectType": "library",
	"release": {
		"version": {
			"manifestRootsToUpdate": ["dist/{projectRoot}"],
			"currentVersionResolver": "git-tag",
			"fallbackCurrentVersionResolver": "disk"
		}
	},
	"tags": [],
	"targets": {
		"test": {
			"executor": "@nx/vitest:test",
			"outputs": ["{workspaceRoot}/coverage/libs/server-express"],
			"options": {
				"config": "libs/server-express/vitest.config.ts"
			}
		},
		"build": {
			"executor": "@nx/esbuild:esbuild",
			"outputs": ["{options.outputPath}"],
			"options": {
				"outputPath": "dist/libs/server-express",
				"main": "libs/server-express/src/index.ts",
				"tsConfig": "libs/server-express/tsconfig.lib.json",
				"assets": ["libs/server-express/*.md"],
				"format": ["cjs"],
				"generatePackageJson": true
			}
		},
		"nx-release-publish": {
			"options": {
				"packageRoot": "dist/{projectRoot}"
			}
		}
	}
}
