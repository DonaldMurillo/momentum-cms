/**
 * MCP (Model Context Protocol) Server Plugin for Momentum CMS
 *
 * Exposes CMS collections, globals, and schemas to AI tools
 * via the Model Context Protocol standard.
 */

export { mcpPlugin } from './lib/mcp-plugin';
export type { McpPluginConfig } from './lib/mcp-plugin.types';
